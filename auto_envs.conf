# add env variables from the host machine, and they will be auto loaded by the script.
# Put only the variable name, not the content
#
# ── default envs
# ────────────────────────────────────────────────────────────
TERM
COLORTERM
# ── user envs
# ────────────────────────────────────────────────────────────
# Add your own envs below. Examples: GH_TOKEN, NPM_TOKEN
# GITHUB_TOKEN
# NPM_TOKEN

# ── agent config env vars
# ────────────────────────────────────────────────────────────
# The config directory env var for the active --agent type is set
# automatically at runtime. Uncomment to forward it explicitly.
# CLAUDE_CONFIG_DIR    # claude-code
